{% extends 'base.html.twig' %}
{% block stylesheets %}
    <link rel="stylesheet" href="{{ asset('assets/css/home.css') }}">
{% endblock %}

{% block body %}
    <div class="container">
        <h1>Tout les studios</h1>
    </div>
    <div class="container">
        <div class="row">
            {% for studio in studios %}

                <div class="col-md-4">
                    <a href="/studio/{{ studio.id }}">
                        <div class="card studio ">
                            <h3 class="card-header">{{ studio.name }}   <span class="badge bg-primary rounded-pill">{{ studio.movies|length }}</span></h3>
                            <i class="card-header fas fa-video"></i>
                        </div>
                    </a>
                </div>


            {% endfor %}
        </div>
    </div>

{% endblock %}